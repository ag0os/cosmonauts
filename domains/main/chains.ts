import type { NamedChain } from "../../lib/chains/types.ts";

/** Main domain does not define static named chains; Cosmo delegates dynamically. */
export const chains: NamedChain[] = [];

export default chains;
