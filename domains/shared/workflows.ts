import type { WorkflowDefinition } from "../../lib/workflows/types.ts";

/** Shared domain provides no workflows by default. */
export const workflows: WorkflowDefinition[] = [];

export default workflows;
