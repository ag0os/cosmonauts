import { describe, test } from "vitest";

describe("backend-neutral attempt liveness", () => {
	// @cosmo-behavior plan:execution-liveness#B-007
	test.todo("applies one liveness contract to every registered backend");

	// @cosmo-behavior plan:execution-liveness#B-019
	test.todo("maps the Drive cap to explicit scheduler hard-timeout policy");
});
