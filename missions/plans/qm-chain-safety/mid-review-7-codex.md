---
kind: independent-review
plan: qm-chain-safety
channel: codex exec -m gpt-6-sol -c model_reasoning_effort=high --sandbox read-only
scope: fd06763..7c35910 (TASK-738..742, rulings, baseline re-anchor) + resolution of mid-review-6, under the D-027 threat model
date: 2026-09-24
---

# Mid-branch review 7 — codex

The prompt is `mid-review-7-prompt.md`. Final message verbatim:

## Verdict: DO-NOT-SHIP-YET for Stages 1–6

The main D-026 boundary is wired through the real callers: the base runtime is exported from the operator repository, the QM and panel use it, and prepare and checks follow reviewer evidence sealing. I found one new accidental failure path that leaves the final report incomplete.

This was a read-only code and test review. I did not run tests, QM, chains, or drive. The checkout remained clean; `git diff --check fd06763..HEAD` reported no whitespace errors.

## Round 6 finding resolutions

“PARTIAL” means the reported route improved but an accidental case or stated limitation remains. Repeated findings from the two reports share a row.

| Round 6 finding | Status | Current evidence |
|---|---|---|
| Claude H-1 / HIGH-1; Codex reviewer runtime override: QM or panel can resolve from reviewed or global definitions | **RESOLVED** | The launcher builds the runtime from the source-exported base with user sources disabled; panel spawn selects that runtime: [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:299), [spawn-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:513). The former global `coding` package is absent on this machine. |
| Codex HIGH; Claude LOW-2: changed check scripts or effective check inputs escape gate ownership | **PARTIAL** | This repository now lists the cited `tsconfig.json`, `biome.json`, `tests/setup.ts`, and runner inputs; changed scripts and config add human items: [config.json](/Users/cosmos/Projects/cosmonauts/.cosmonauts/config.json:24), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:438). The mechanism still depends on each project’s configured list covering its effective inputs. |
| Codex LOW / Claude L-1: source or external store paths in the operator note | **RESOLVED** | Source, store, and run roots are supplied to the redactor before the note reaches the prompt: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:239), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1240). |
| Claude L-2: abandoned-write reason replaces the original deadline | **RESOLVED** | The sealing reason is appended to the existing failure reason: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:917). |
| Claude L-3: disposition missing from status, QM-specific kind, removal default untested | **RESOLVED** | Generic `artifact-disposition` is recorded and rendered by status; removal reads the base-configured timeout: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1046), [run-control-tools.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/run-control-tools.ts:118). |
| Claude L-4: no in-progress reviewer `artifact_written` event | **RESOLVED by D-025** | Reviewer refs are published through the completed step, which status reads: [artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:225), [controller.ts](/Users/cosmos/Projects/cosmonauts/lib/durable-runtime/controller.ts:82). |
| Earlier Codex C1: source/store path in the caller note | **RESOLVED** | The caller note is redacted before launch uses it: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:239). |
| Earlier Codex C4: direct specialist instructions and installed-definition shadowing | **RESOLVED for the current installation** | Bundled prompts cover both host materials and direct `cody`/`cosmo` scope; the quality runtime excludes user sources: [reviewer.md](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/reviewer.md:49), [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:314). |
| Earlier Claude M4: a separately detached grandchild can survive check cleanup | **PARTIAL** | The host kills the check’s process group, but a separately detached process can outlive it: [quality-review-command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:58). Accidental daemon or watcher creation remains possible. |
| Earlier Claude L4: caller prompt and requested model | **PARTIAL** | Caller text reaches the shared launcher: [chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:515). Requested-model calibration belongs to the excluded Stage 7 work. |
| Earlier Claude L5: injectable `execute` port | **PARTIAL, fail closed** | The port remains for injected callers, while absence of a host-observed bound gate blocks `ready`: [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:290), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:741). |
| Earlier Claude NEW-L2: retained clone after an unsettled QM session | **PARTIAL** | The wait has a grace period; a still-live session causes retention, with no later reclamation in this slice: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:541), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1014). |
| Earlier Claude NEW-L4: omitted panel skills | **PARTIAL** | Unavailable base skill paths are recorded and omitted, rather than supplied: [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:86). |
| Round-2 L5 / Claude LOW-3: materials can be rewritten after verification | **RESIDUAL** | Materials are verified before checks; deliberate post-seal rewriting is the hostile-only limit recorded by D-027: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:601), [plan.md](/Users/cosmos/Projects/cosmonauts/missions/plans/qm-chain-safety/plan.md:584). |
| Round-3 NEW-L3: JSX text can be mistaken for a suppression comment | **OPEN** | The comment-range scan remains: [suppression-policy.ts](/Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:36). It fails closed. |
| Codex HIGH-1: surviving check rewrites base runtime or materials before review | **RESIDUAL** | D-026 now constructs and uses the base runtime and seals reviewer evidence before checks. Deliberate later tampering remains under D-027: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1141), [plan.md](/Users/cosmos/Projects/cosmonauts/missions/plans/qm-chain-safety/plan.md:603). |
| Codex HIGH-2: cited effective check configuration omitted | **RESOLVED for this repository** | The cited inputs are now gate-owned: [config.json](/Users/cosmos/Projects/cosmonauts/.cosmonauts/config.json:25). The general configuration dependency noted above remains partial. |
| Codex MEDIUM: base-runtime Git work continues after cancellation or deadline | **PARTIAL** | Git export now receives the abort signal; asynchronous runtime import can still be settling when the host retains the workspace: [workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:237), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:337). |
| Codex LOW: base-runtime regression test bypasses the launcher | **RESOLVED** | The newer test calls `launchQualityReview` and asserts the launcher-supplied registry resolves the base definition: [run.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:3293). |
| Claude HIGH-2: checks tamper with clone Git objects before base export | **RESOLVED for that route** | The base export is now cloned from the operator source before checks: [workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:237), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1147). |
| Claude HIGH-3: clone `.pi` settings execute commands or append prompt text | **RESOLVED** | Quality sessions use untrusted in-memory settings and an empty appended system prompt: [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:169). |
| Claude LOW-1: duplicate `package.json` human item | **RESOLVED** | Gate-owned paths are deduplicated: [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:438). |
| Claude LOW-2: shorthand and lifecycle scripts evade the script detector | **RESOLVED for this repository; PARTIAL generally** | `package.json` itself is gate-owned here, and script-object changes are compared. Other projects must configure their effective inputs: [config.json](/Users/cosmos/Projects/cosmonauts/.cosmonauts/config.json:25), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1304). |

## New findings in `fd06763..HEAD`

**MEDIUM — Successful analysis preparation disappears from a failure report.** The new pre-assessment `analysisPrepare` path records successes in `analysisPreparationLines` ([run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:420)). If an ordinary QM session then fails, `recordAssessmentFailure` writes a generic “Not run” checks artifact and builds its final Checks section only from `checkResults`, which are still empty ([run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:852)). For example, `bun install --ignore-scripts` succeeds, then model initialization fails: neither the final report nor `checks.md` says that preparation ran. If a second configured preparation step fails, prior successful steps are likewise lost because lines are appended only after the whole call returns. This breaks **D-026’s explicit requirement to record analysis preparation** and **AC-007/AC-008’s complete exit report**. The added test covers only a successful assessment ([launch.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-launch.test.ts:598)).

**LOW — The post-check sealing test cannot detect loss of sealing.** The test named “does not let a later check rewrite change sealed review evidence” supplies no reviewer artifact, never checks reviewer bytes or the post-check materials, and asserts only that the check appears in the report and a *pre-assessment* integrity message is absent ([run.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:3078)). An accidental refactor that skips `sealReviewerEvidence` would leave those assertions satisfied. This weakens the regression guard for **D-026 step 6 / B-003**; it is a test finding, not evidence that sealing is currently skipped.

I found no additional HIGH defect. In particular, the TASK-740..742 extractions preserve the inspected host-report text, chain episode calls, stage result shape, and event order relative to `c962b94`. The existing inline chain emits its QM `stage_start` after the launcher returns in both versions ([chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:509)); this is unchanged.

## End-to-end and residual limits

The CLI, direct `spawn_agent`, inline `chain_run`, and durable terminal QM stage converge on `launchQualityReview`: [CLI](/Users/cosmos/Projects/cosmonauts/cli/main.ts:652), [spawn](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:808), [inline chain](/Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:509), [durable chain](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:297). `/agent quality-manager` refuses an interactive switch ([agent-switch](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/agent-switch/index.ts:57)). `run status` reads the final report reference and disposition ([controller.ts](/Users/cosmos/Projects/cosmonauts/lib/durable-runtime/controller.ts:60), [run-control-tools.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/run-control-tools.ts:118)).

At the launch boundary, consent is bound to the private checkout ([run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:329)); model-facing quality prompts receive the private materials path and redacted caller note, while source and host-store paths remain host context ([launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:331), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:239)). The base is computed with `merge-base` ([workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:392)); `ready` requires the observed bound audit ([launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:22), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:741)). The ordering puts assessment and evidence sealing before prepare and checks ([run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:576)). The host’s intended operator-checkout write is the D-018 plan summary ([run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1442)). All four bundled specialist prompts describe both QM materials and direct `cody`/`cosmo` scope; see [security-reviewer.md](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/security-reviewer.md:86).

**Residual limits (hostile-only):** deliberate post-seal rewriting of materials or the base export; deliberate escape from a check’s process group; deliberate tampering with operator Git objects. These are unranked under D-027. Host-run checks executing reviewed code with operator authority are accepted by D-028.

Timed-out panel children are still not cancelled, there is still no general persisted-evidence contract, and the stage summary remains capped at 200 characters ([assistant-text.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/assistant-text.ts:34)). Stages 7–8 were excluded from this verdict.
