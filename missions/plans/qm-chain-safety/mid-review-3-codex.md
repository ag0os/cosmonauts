---
kind: independent-review
plan: qm-chain-safety
channel: codex exec -m gpt-6-sol -c model_reasoning_effort=high --sandbox read-only
scope: round-2 remediation a978dbc..4c7bb15 (TASK-733, TASK-734) + resolution of mid-review-2
date: 2026-09-24
---

# Mid-branch review 3 — codex

The prompt is `mid-review-3-prompt.md`. Final message verbatim:

## Resolution of prior findings

I reviewed `a978dbc..HEAD` and the cited tests without changing files, running the QM, or running a chain. **RESOLVED** means the reported failure path is closed in code; it does not imply an end-to-end test was run here.

| Finding from the two round-2 reviews | Status | Current evidence |
|---|---|---|
| Round-1 Codex C1 / Claude H2; round-2 Claude M1 — source or host paths in quality prompts | **RESOLVED** for the reported paths | The QM prompt uses the materials copy; panel skill paths beneath the source root are remapped into the clone. The new full-system-prompt test checks both paths. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:243), [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:60), [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-session-prompt.test.ts:8) |
| C2 / H1 — clone analysis consent unbound | **RESOLVED** | Run-scoped consent is created for the snapshot and injected into project-tools discovery. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:245), [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:119), [project-tools](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/project-tools/index.ts:556) |
| C3 — unbound or unobserved audit can yield `ready`; round-2 Codex N2 / Claude L1 missing-state bypass | **RESOLVED** | The launcher validates observed status and audit results; the host blocks `ready` unless gate state is `completed-bound`, including through injected `execute`. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:20), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:488) |
| C4 / Claude M2 — specialist procedures and direct spawns | **RESOLVED** in bundled prompts | The prompts retain review dimensions and separate host-material and direct-spawn scope instructions. [reviewer.md](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/reviewer.md:49), [security-reviewer.md](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/security-reviewer.md:86) |
| C5 — markerless `chain_run` / `run_driver` | **RESOLVED** | Both refuse an unresolved caller. [chain-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/chain-tool.ts:127), [driver-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/driver-tool.ts:195) |
| C6 / Claude M1 — host triage misses CLI changes or prevents QM additions; round-2 Claude L4 overbroad triage | **PARTIAL** | The QM can add a lens and prose-only additions are filtered, but the new filter misses deletion-only changes; see new finding 3. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:223), [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:292) |
| C7 / Claude M3; round-2 Codex N1 — assessment deadline, cancellation, unsettled QM | **RESOLVED** for the reported case | Deadline/cancellation reaches the session; after bounded settlement, an unsettled QM is named and its workspace retained. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:340), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:394), [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:1644) |
| Claude H3 — base was ref tip | **RESOLVED** | Materials and checks use the computed merge-base. [workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:302) |
| Claude M4 — timeout descendants; accepted own-process-group grandchild | **PARTIAL** | Same-group descendants are killed, but completion still waits for `close`; a grandchild in its own group can retain a pipe. [command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:67), [command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:102) |
| Claude M5 — cleanup failure overwrote a verdict; round-2 Claude L3 lifecycle/digest | **RESOLVED** for throwing removal | The report is retained and annotated; one terminal phase records the final digest and disposition. The new cleanup ordering has a separate liveness defect below. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:682), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:710) |
| Claude M6; round-2 Claude H1 — suppression forms and template-substitution regression | **RESOLVED** | Parser-based comment collection is restored. Tests cover substitutions, regex literals, JSX forms, and comparison with the tracked exception registry. [suppression-policy.ts](/Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:36), [test](/Users/cosmos/Projects/cosmonauts/tests/quality/suppression-policy.test.ts:126), [registry test](/Users/cosmos/Projects/cosmonauts/tests/quality/suppression-policy.test.ts:146) |
| Claude L1 — own plan summary enters review | **RESOLVED** | Capture excludes this run’s summary. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:231) |
| Claude L2 — model-authored `refused` | **RESOLVED** | It becomes report-integrity failure. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:460) |
| Claude L3; round-2 Codex N3 / Claude L2 — late reviewer artifact after finalization | **RESOLVED** for late arrivals | The host seals reviewer writes before finalization; the detached-child completion checks the seal. A test drives completion after sealing and confirms no reviewer artifact or event. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:655), [spawn-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:189), [test](/Users/cosmos/Projects/cosmonauts/tests/extensions/orchestration.test.ts:1227) |
| Claude L4 — caller parameters dropped | **PARTIAL** | Panel timeout is configurable, but CLI `--print` still discards its required prompt, and QM `spawn_agent` discards its prompt and model. [CLI](/Users/cosmos/Projects/cosmonauts/cli/main.ts:623), [spawn-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:748) |
| Claude L5 — production `execute` port; round-2 Codex N2 bypass | **PARTIAL** | The port remains in production types, but absent gate state now blocks `ready`; the reported safety bypass is closed. [types.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/types.ts:123), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:488) |
| Claude L6 — unrelated config edit gate-owned | **RESOLVED** | The extra item requires a changed `qualityReview` block. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:310), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:850) |
| Claude L7 — `/agent quality-manager` bypass | **RESOLVED** | Typed selection is refused and the picker excludes the role. [agent-switch](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/agent-switch/index.ts:50) |
| Claude L8 — generic `StepResult.childRun` | **RESOLVED as accepted scope** | It carries the nested run reference from the durable QM stage. [durable-chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:334) |
| Round-2 Claude M2 — detached host checks survive CLI Ctrl-C | **RESOLVED** for handled SIGINT/SIGTERM | CLI cancellation now aborts the run; active process groups have signal and exit handlers. The test invokes the kill handler, though it does not send a real terminal signal. [CLI](/Users/cosmos/Projects/cosmonauts/cli/main.ts:629), [command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:11), [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-command.test.ts:10) |
| Round-2 Claude L1 — failed-audit reporting | **PARTIAL** | Duplicate text is fixed and failure blocks `ready`, but a bound failing audit is now omitted from human decisions, contrary to D-025; see new finding 4. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:507) |
| Round-2 Claude L5 — materials writable during checks | **OPEN, accepted residual** | Materials are sealed only after preparation and checks. [workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:385), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:320) |
| Round-2 Claude L6 — analysis results in ordinary spawn events | **RESOLVED** | Result capture is conditional on a quality-review context. [agent-spawner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/agent-spawner.ts:428) |

## New findings in `a978dbc..HEAD`

1. **HIGH — Sealing can defeat the assessment deadline.** `sealReviewers()` waits without a deadline for every reviewer write already in progress. If a child starts `writeReviewer()` and its store operation stalls, the QM deadline and 250 ms session grace can expire, yet the host remains stuck at sealing and never reaches a terminal report or lifecycle event. The late-reviewer test starts completion *after* sealing, so it cannot detect this case. This breaks B-004 and D-025’s bounded-assessment outcome. [artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:134), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:394), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:655), [test](/Users/cosmos/Projects/cosmonauts/tests/extensions/orchestration.test.ts:1227)

2. **HIGH — Workspace removal now precedes the terminal lifecycle event.** The remediation writes `final.md`, awaits recursive removal, and only then appends `finalized` or `retained`. If removal never settles, `run status` remains nonterminal and the persisted report can still be the provisional failure record after a completed assessment. The ratified Design §3 orders final report, terminal event, then removal. Tests cover successful and throwing removal, not a pending remover. This breaks B-004 and INV-003’s durable outcome on interruption. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:682), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:690), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:715), [plan.md](/Users/cosmos/Projects/cosmonauts/missions/plans/qm-chain-safety/plan.md:838)

3. **MEDIUM — A deletion-only change loses host-required specialist triage.** `triageReviewLenses()` returns only `reviewer` whenever there are no added code lines. Deleting an authorization check from `lib/auth.ts`, for example, has no added line, so security review is no longer host-required even though the filename identifies the surface. The new tests exercise added lines and prose, not deletion-only diffs. This breaks B-006 and D-025’s host minimum. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:292), [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:314), [triage tests](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-launch.test.ts:158)

4. **MEDIUM — Failed-audit classification conflicts with the ratified decision.** D-025 requires a failed audit to be recorded as a human-decision item. The new branch deliberately excludes `Analysis audit gate state: fail` from that section and inserts the generic finding “Analysis audit found introduced debt,” without B-005’s finding details such as `file:line`, severity, or suggested fix. Its test asserts the absence of the human item, so it would pass while this contract remains broken. The host still prevents `ready`. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:507), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:536), [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:185), [D-025](/Users/cosmos/Projects/cosmonauts/missions/plans/qm-chain-safety/plan.md:510)

## Caller check and accepted risks

CLI print and interactive QM, QM `spawn_agent`, inline and durable terminal chain stages all route through the shared launcher; `/agent quality-manager` is refused. The normal launcher binds clone consent, uses the merge-base, gives the QM materials paths, and validates host-observed audit results. `run status` points to `qm/final.md`. The bundled specialist prompts serve both panel and direct `cody`/`cosmo` spawns. The operator checkout is only written for the D-018 plan summary by the reviewed path. [CLI](/Users/cosmos/Projects/cosmonauts/cli/main.ts:628), [spawn-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:748), [inline chain](/Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:443), [durable chain](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:334), [status](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/run-control-tools.ts:118)

One current-machine qualification matters: the installed `coding` package contains an older QM definition, and installed packages take precedence over bundled ones. Its old prompts can therefore be selected by a live caller on this machine until that installation is refreshed. The shared launcher and restricted tool profile still apply; the bundled-prompt conclusion above is about this branch’s files. [runtime.ts](/Users/cosmos/Projects/cosmonauts/lib/runtime.ts:36), [installed definition](/Users/cosmos/.cosmonauts/packages/coding/agents/quality-manager.ts:3)

The coordinator’s **materials-writable** acceptance is sound only under the plan’s trust assumption that configured host checks are trusted; the writable interval remains real. The **own-process-group grandchild** acceptance is not sound as a claim of bounded check completion, because `close` can remain pending; it remains a prior liveness risk. Accepting **model** propagation for Stage 7 is sound, but accepting the **prompt** drop is not: CLI print mode requires a prompt and then discards it, and QM spawns do the same.

**Verdict: DO-NOT-SHIP-YET for Stages 1–6 as remediated.** The two new unbounded waits are sufficient blockers. I did not execute tests in this read-only review; I inspected their assertions and confirmed `git diff --check a978dbc..HEAD` is clean. Stages 7–8 are outside this verdict.
