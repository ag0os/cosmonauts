---
kind: independent-review
plan: qm-chain-safety
channel: codex exec -m gpt-6-sol -c model_reasoning_effort=high --sandbox read-only
scope: round-5 remediation 0f71cb5..(TASK-737 d6dd6c7) + resolution of mid-review-5
date: 2026-09-24
---

# Mid-branch review 6 — codex

The prompt is `mid-review-6-prompt.md`. Final message verbatim:

## Resolution of reported findings

| Finding | Status now | Evidence |
|---|---|---|
| Claude H-1 — reviewed domains or agents can choose the reviewer runtime | **PARTIAL** | The launcher now builds the runtime from a separate checkout of the merge-base ([launch](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:248>), [materialization](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:225>)). But runtime discovery still includes global packages, which override bundled definitions ([scanner](</Users/cosmos/Projects/cosmonauts/lib/packages/scanner.ts:65>)). On this machine the installed QM is still the old remediation-capable definition ([installed definition](</Users/cosmos/.cosmonauts/packages/coding/agents/quality-manager.ts:3>)). The host’s tool and spawn restrictions limit its authority, but its prompt and behavior are not the bundled review-only version. |
| Codex HIGH — a changed `package.json` script selects a check program | **PARTIAL** | A changed named script now becomes a human item and blocks `ready` ([script comparison](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1019>), [ready gate](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:550>)); the test covers a no-op `test` script ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2772>)). Checks still execute before that classification ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:323>)); other effective check inputs remain unlisted, as detailed below. |
| Codex LOW / Claude L-1 — external store root and `~` paths in the operator note | **RESOLVED for the reported forms** | The actual store root is included in redaction and home-relative forms are replaced ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:228>), [redactor](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:957>)); the test uses an external store ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2975>)). |
| Claude L-2 — abandoned-write reason replaces the original failure | **RESOLVED** | The abandonment reason is appended ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:717>)); the deadline test asserts both reasons ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:3012>)). |
| Claude L-3 — disposition absent from `run_status`, QM-specific kind, untested removal default | **RESOLVED** | Status text includes disposition ([tool](</Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/run-control-tools.ts:118>)); the record uses generic `artifact-disposition` ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:823>)); removal timeout is read from base config ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:805>)) and exercised through config ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:45>)). |
| Claude L-4 — reviewer refs no longer emit an in-progress artifact event | **RESOLVED by the recorded D-025 amendment** | D-025 explicitly makes `StepResult` the publication path ([decision](</Users/cosmos/Projects/cosmonauts/missions/plans/qm-chain-safety/plan.md:545>)); status also reads step output artifacts ([controller](</Users/cosmos/Projects/cosmonauts/lib/durable-runtime/controller.ts:82>)). |
| Earlier Codex C1 — source/store paths reach a quality prompt | **RESOLVED for the reported caller-note path** | The source and store roots are redacted before the note is added to the QM prompt ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:228>), [launch](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:303>)). |
| Earlier Codex C4 — direct-spawn specialist procedures; installed definitions | **PARTIAL** | Bundled specialist prompts describe both host-material and direct `cody`/`cosmo` scope ([generalist](</Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/reviewer.md:5>), [performance](</Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/performance-reviewer.md:83>)). The global installed package still shadows bundled definitions and prompts on this machine ([scanner](</Users/cosmos/Projects/cosmonauts/lib/packages/scanner.ts:65>)). |
| Earlier Claude M4 — detached grandchild | **PARTIAL, accepted narrowly** | Each host command kills its process group and finishes on leader exit ([command](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:93>)). A separately detached grandchild can survive. The accepted claim about the **host wait** is sound; it is not a claim that all descendants are gone. |
| Earlier Claude L4 — caller prompt and model | **PARTIAL** | Caller text reaches the launcher from CLI, spawn, inline chain and durable chain ([CLI](</Users/cosmos/Projects/cosmonauts/cli/main.ts:653>), [spawn](</Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:748>), [inline](</Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:443>), [durable](</Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:334>)). Requested-model behavior remains Stage 7 scope. |
| Earlier Claude L5 — injectable `execute` port | **PARTIAL, fail-closed** | The port remains ([launch](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:238>)); without observed bound gate state, `ready` is blocked ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:550>)). |
| Earlier Claude NEW-L2 — retained clone after an unsettled session | **PARTIAL** | The assessment wait has a grace period ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:443>)), then retains a workspace when work is still live ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:770>)). No later reclamation is shown in this slice. |
| Earlier Claude NEW-L4 — omitted panel skills | **PARTIAL** | Missing clone skill directories are recorded ([factory](</Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:65>)); recording an omission does not supply the skill. |
| Round-2 L5 — materials writable during checks | **PARTIAL** | A synchronous rewrite is now caught by the digest check ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:384>), [test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2916>)). The verification is before later runtime setup and the QM read; a surviving detached process can rewrite materials afterward. Accepting writable materials as a *trustworthy-evidence guarantee* is therefore unsound. |
| Round-3 NEW-L3 — JSX text false positive | **OPEN, accepted soundly** | The parser can overclassify JSX text ([scanner](</Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:42>)); this fails closed rather than allowing a new suppression silently. |

## New findings in `0f71cb5..HEAD`

1. **HIGH — A surviving check process can choose what the host imports and what the QM reads.** The new `base-runtime` directory is created beside the checkout at a predictable path ([workspace](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:230>)). Its project modules are imported into the host after checks ([launch](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:248>), [loader](</Users/cosmos/Projects/cosmonauts/lib/domains/loader.ts:99>)). A changed test or install hook can leave a separately detached process, which the accepted process-group cleanup does not kill, and have it replace `../base-runtime/.cosmonauts/domains/.../domain.ts` after checkout but before import. The same process can alter `../materials/full.diff` after the one verification at [run:384](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:384>). This is a specific route by which the **change chooses host-imported reviewer code or review evidence**, contrary to D-025 and B-002/B-003. It is distinct from N-004’s general lack of an OS sandbox. The synchronous rewrite test cannot detect the later race ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2916>)).

2. **HIGH — The new executed-code gate list omits effective check configuration.** The base config lists `package.json`, `bun.lock`, the Vitest runner and `vitest.config.ts` ([config](</Users/cosmos/Projects/cosmonauts/.cosmonauts/config.json:24>)); matching is exact ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1005>)). Yet `vitest.config.ts` loads `tests/setup.ts` ([config](</Users/cosmos/Projects/cosmonauts/vitest.config.ts:3>)), `tsc --noEmit` reads `tsconfig.json` ([package](</Users/cosmos/Projects/cosmonauts/package.json:34>)), and `biome check .` reads `biome.json` ([package](</Users/cosmos/Projects/cosmonauts/package.json:29>)). A change to those unlisted configuration or setup files can alter or weaken a configured check without a gate-owned human item. The added tests vary only a named package script or an explicitly listed runner ([tests](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2772>), [runner test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2831>)). This breaks D-025’s executed-code amendment and B-008/INV-005. Even for a *listed* changed script, the host executes it before recording the human item ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:323>)); the item blocks `ready` but does not prevent change-selected execution.

3. **MEDIUM — Base-runtime creation continues after assessment cancellation or deadline.** The new materialization runs three Git commands with no `AbortSignal` ([workspace](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:225>), [Git wrapper](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:73>)). It runs inside the assessment promise; the outer deadline races and can finalize while that promise remains live ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:397>), [run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:443>)). With a short configured deadline or caller cancellation during a slow clone, Git and subsequent runtime setup may continue after the failed/cancelled verdict, retaining the workspace. The host wait remains bounded, but D-025’s bounded-assessment and cancellation behavior is incomplete for this new setup step. This does **not** concern timed-out panel children, which AC-015 leaves uncancelled.

4. **LOW — The base-runtime regression test does not exercise the launcher connection.** It calls `materializeBaseReviewProject` and `CosmonautsRuntime.create` directly ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:3067>)). Reverting the actual launcher’s `projectRoot` at [launch:260](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:260>) to the reviewed clone would leave that test green. The separate session-factory test mocks `buildSessionParams` and invokes the supplied config loader itself ([test](</Users/cosmos/Projects/cosmonauts/tests/orchestration/session-factory.security.test.ts:118>)). The main D-025 reviewer-runtime wiring therefore lacks a test that would fail on that regression.

## End-to-end verdict

**DO-NOT-SHIP-YET for Stages 1–6 as remediated.** The ordinary caller routes reach the shared launcher, the snapshot computes a merge-base ([workspace](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:307>)), analysis consent is bound to the clone ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:278>), [tool](</Users/cosmos/Projects/cosmonauts/domains/shared/extensions/project-tools/index.ts:556>)), and `ready` requires a host-observed bound audit ([validator](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:22>), [gate](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:550>)). A bound failing audit is treated as gate failure with findings, not a human item ([launch](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:319>), [run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:569>)). `/agent quality-manager` is refused ([switch](</Users/cosmos/Projects/cosmonauts/domains/shared/extensions/agent-switch/index.ts:57>)); `run status` exposes the report and disposition ([controller](</Users/cosmos/Projects/cosmonauts/lib/durable-runtime/controller.ts:60>), [tool](</Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/run-control-tools.ts:118>)). Host code’s sole intended operator-checkout write is the D-018 plan summary ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1156>)). The 200-character stage-summary limit remains ([run](</Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:862>)); no general evidence contract or panel-child cancellation was added.

This is a read-only source-and-test review. I did not run the QM, a chain, Drive, or tests; `git diff --check` found no whitespace errors and the working tree was clean. The analysis skill’s `analysis_status` tool was unavailable, so I claim no structural-analysis result. Stages 7–8 and the already escalated N-004 are outside this verdict.
