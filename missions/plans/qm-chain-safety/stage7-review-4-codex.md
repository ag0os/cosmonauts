# Stage 7 review 4 — codex

The prompt is `stage7-review-4-prompt.md`. Reviewed HEAD `acda9d1` (TASK-749 at `3ac2e24`). Final message verbatim:

## Stage 7 verdict: **DO-NOT-SHIP-YET**

TASK-749 fixes the named duplicate-entry and ID-matching cases, but the B-010 host calibration still permits a false `ready` and can leave an unsupported `P0` visible in Findings.

### Findings

1. **HIGH — An indented subfinding can reach `ready`** ([report.ts:205](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-report.ts:205), [report.ts:228](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-report.ts:228), [run.ts:947](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:947)). In a scratch probe, Findings contained `- F-1 dismissed`, then an indented `- F-2 P2 null input crashes`, then a final `closureEvidence:` line for F-1. The parser treated all three lines as one closed F-1 entry: `openFindings=false`, no calibration issue, and no unaccounted Findings text. With clean host gates, checks, and model evidence, the ready check permits `ready` despite F-2. This breaks **D-031 floor 1**. If F-2 also appears in a reviewer artifact, the carry-over catches it; the failing case is a finding introduced in the QM report.

2. **MEDIUM — A cap can rewrite Gates instead of Findings** ([report.ts:53](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-report.ts:53), [run.ts:913](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:913)). For an unindexed report with the same `- PF-1 P0 slow batch path` line in Gates and Findings, the first-match replacement changes Gates to P2 and leaves Findings at P0. The host records a “capped at P2” Findings note, with no human item. The scratch probe reproduced both visible sections. This breaks **D-031’s P0/P1 cap**.

3. **LOW — An ordinary multiline evidenced dismissal blocks a clean `ready`** ([models.ts:310](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-models.ts:310), [models.ts:137](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-models.ts:137)). `F-1 dismissed; closureEvidence: fixed in lib/a.ts` followed by an indented `file: lib/a.ts:1` line is treated as open and produces a human item, even when the cited text is in the materials. The citation matcher requires the evidence to be at the end of the entire multiline entry. This breaks **TASK-749 AC #1’s evidenced-dismissal ready path** and the requested liveness check.

### Review-3 dispositions

| Review-3 item | Status | Evidence and pre-TASK-749 check |
|---|---|---|
| Codex 1, non-bullet Findings | **PARTIAL** | The new unaccounted-text guard catches the named numbered, `*`, `+`, and prose shapes ([report.ts:205](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-report.ts:205)); the indented subfinding above remains. The [run test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:1298) would fail on pre-TASK-749 code for its numbered case. |
| Codex 2, dismissal closes another entry | **RESOLVED** | Closure is evaluated per Findings entry ([models.ts:137](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-models.ts:137)); mixed closed/open observations raise a human item ([models.ts:207](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-models.ts:207)). The [two-section test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-models.test.ts:313) failed on pre-TASK-749 code. |
| Codex 3, duplicate observation P0 | **RESOLVED for the named case** | A high-priority observation now raises a human item ([models.ts:243](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-models.ts:243)); its [unit test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-models.test.ts:332) failed before TASK-749. Finding 2 is a separate remaining cap route. |
| Claude H-1, false `ready` | **PARTIAL** | The named non-bullet shapes are guarded ([run.ts:949](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:949)); the indented shape in finding 1 is not. |
| Claude L-1, leading tokens and decoration | **RESOLVED** | The matcher skips `TASK`/`AC`/`D`/`INV`/`B` tokens and accepts the tested decoration ([models.ts:282](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-models.ts:282)); the QM prompt asks for leading reviewer IDs ([prompt:35](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/quality-manager.md:35)). The [new unit cases](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-models.test.ts:283) failed before TASK-749. |
| Claude L-2, `$` expansion | **RESOLVED** | A replacement callback preserves literal `$` text ([report.ts:59](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-report.ts:59)); all four [unit cases](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-report.test.ts:116) failed before TASK-749. |
| Claude L-3, diversity dedup test | **RESOLVED as coverage** | The new [report-and-summary assertion](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:1573) checks one occurrence where the two sources coincide; the [Set](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:857) dedupes them. This test would **pass** on pre-TASK-749 code because that Set already existed; removing it would fail the assertion. |
| Claude L-4, surviving mutations | **RESOLVED for the pure model mutations** | Scratch mutations of reviewer-section P0, unmapped P0, the observation scan, other-lens closure, and the ID-less block each failed a targeted [model test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-models.test.ts:344). The host ready-condition mutation has source-level evidence from the [numbered-shape test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:1298), but I could not execute that mutation test under the run prohibition. |

The pure model/report tests passed **49/49** on HEAD; **19 failed** against the pre-TASK-749 modules. Replacing the host Findings condition with `false` would let the numbered case in the run test retain `ready`, so its `not-ready` assertion would fail. That is a control-flow check, **not an executed mutation result**.

### Regression checks and residuals

- **Stages 1–6 and B-011:** No regression found in the inspected paths. Host gate and human-item blocks remain at [run.ts:934](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:934); assessment precedes sealed evidence and host checks at [run.ts:1345](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1345); D-019 items remain at [run.ts:973](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:973); base-owned runtime and config remain at [launch.ts:301](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:301) and [run.ts:1467](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1467). Clean, all-out-of-range, and single evidenced-dismissal reports passed pure ready-path probes.
- **Audit:** Direct Fallow changed-scope audit on a scratch HEAD snapshot against merge-base `29fc0ce`, using all three committed baselines, returned **pass, zero introduced issues**. Scratch typecheck, changed-file Biome, and `git diff --check` passed. The diff stays within TASK-749’s code, prompt, tests, and task record.
- **Recorded text-recognition limits:** A measurement-looking code line and a QM repetition of same-lens closure evidence remain D-031 heuristic residuals.
- **Hostile-only routes, unranked:** Deliberate tampering with reviewed-code checks, materials, Git objects, or fabricated and colluding review evidence remains outside D-027.
- **Execution limit:** Automatic approval review rejected the copied `quality-review-run` suite because it exercises the QM lifecycle, which your instruction prohibited. I did not run it or the full suite. Executed confirmation of the host-condition and diversity-dedup mutations would require you to authorize that narrowly scoped lifecycle test.
