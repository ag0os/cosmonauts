---
kind: independent-review
plan: qm-chain-safety
channel: codex exec -m gpt-6-sol -c model_reasoning_effort=high --sandbox read-only
scope: round-4 remediation ea1fa2d..(TASK-736 a7136e0) + resolution of mid-review-4
date: 2026-09-24
---

# Mid-branch review 5 — codex

The prompt is `mid-review-5-prompt.md`. Final message verbatim:

# `qm-chain-safety` review — Stages 1–6

**Verdict: DO-NOT-SHIP-YET.** TASK-736 fixes the reported round-4 failures in the ordinary caller path, but the base-owned command list does not stop a reviewed change from choosing the code those commands execute. The acceptance of writable review materials is also unsound as a guarantee of trustworthy review evidence. I did not run the QM, a chain, or Drive.

## Resolution of review-4 findings

“Partial” distinguishes a fix that works for the default caller path from the broader claim in D-025.

| Finding from the two review-4 files | Status now | Code evidence |
|---|---|---|
| Codex C1 / Claude H2 / round-2 M1; Codex NEW-1 / Claude NEW-M3 — source or store paths reach a quality prompt | **PARTIAL** | The caller note is redacted before assessment and report output in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:227), [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:931). It redacts `projectRoot` and `run.runDir`, but not an external store’s *root*; see LOW finding below. |
| Codex C2 — clone analysis consent | **RESOLVED** | Run-scoped authorization is made for the clone and supplied to discovery in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:273) and [project-tools/index.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/project-tools/index.ts:556). |
| Codex C3 / round-2 N2 / L1 — `ready` without an observed, bound audit | **RESOLVED** | The host checks the observed binding, result kind, verdict and literal base in [quality-review-launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:21); any other gate state blocks `ready` in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:525). |
| Codex C4 / Claude M2 — specialist procedures and direct spawns | **RESOLVED in bundled prompts; PARTIAL on this machine** | The bundled [generalist](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/reviewer.md:47), [security](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/security-reviewer.md:86), [performance](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/performance-reviewer.md:84), and [UX](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/ux-reviewer.md:83) prompts cover QM materials and direct `cody`/`cosmo` scope. The installed package still has the old write-capable [QM definition](/Users/cosmos/.cosmonauts/packages/coding/agents/quality-manager.ts:3) and has precedence under [runtime.ts](/Users/cosmos/Projects/cosmonauts/lib/runtime.ts:35). |
| Codex C5 — markerless chain and Drive authority | **RESOLVED** | Unresolved callers are rejected by [chain-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/chain-tool.ts:127) and [driver-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/driver-tool.ts:195). |
| Codex C6 / Claude M3 / round-2 L4 / round-3 Codex #3 / Claude NEW-M2 — host lens triage | **RESOLVED for the reported files** | Changed and removed behavioral lines are considered; the round-4 filter includes capability and template Markdown and root agent guidance in [quality-review-launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:346). |
| Codex C7 / Claude M3 / round-2 N1 — deadline and live QM | **RESOLVED for the reported wait** | Assessment abort and bounded settlement are in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:365); live work retains its workspace at [line 714](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:714). |
| Claude H3 — ref tip used as base | **RESOLVED** | The clone computes `merge-base` before writing materials and the diff in [quality-review-workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:302). |
| Claude M4 — check wait held by a grandchild | **RESOLVED for the host wait; PARTIAL for descendants** | The command completes on leader `exit` and now kills its process group in [quality-review-command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:93). A separately detached grandchild can still survive. |
| Claude M5 / round-2 L3 / round-3 Codex #2 — cleanup and terminal ordering; Claude NEW-L1 | **RESOLVED** | Final report and lifecycle precede removal in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:742). The QM-specific terminal interpretation was removed from [status.ts](/Users/cosmos/Projects/cosmonauts/lib/durable-runtime/status.ts:3), allowing normal `run_*` terminal events. |
| Claude M6 / round-2 H1 — suppression forms and template regression | **RESOLVED** | Parser-based comment collection remains in [suppression-policy.ts](/Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:36). |
| Claude L1 — own plan summary enters capture | **RESOLVED** | Capture excludes this run’s summary in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:255). |
| Claude L2 — model-authored `refused` | **RESOLVED** | It becomes a report-integrity failure in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:495). |
| Claude L3 / round-2 N3 and L2; Codex NEW-3 / Claude NEW-L4 event residue — late reviewer artifact event | **RESOLVED for the reported race** | Reviewer writes no longer emit `artifact_written` before sealing; accepted references enter the completed step result in [quality-review-artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:214) and [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:820). The delayed-append test would fail if that event were restored: [quality-review-artifacts.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-artifacts.test.ts:122). |
| Claude L4 / round-3 L4 — caller prompt and model | **PARTIAL** | Caller text reaches CLI, spawn and chain launchers; durable chains now pass `stage.prompt` rather than generated boilerplate in [durable-chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:333). Requested model propagation remains Stage 7 scope. |
| Claude L5 — production `execute` port | **PARTIAL, fail-closed** | The port remains at [quality-review-launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:239), but its absent observed gate state blocks `ready` at [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:525). |
| Claude L6 — unrelated config edit treated as gate-owned | **RESOLVED** | The host compares the `qualityReview` blocks before adding the item in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:342) and [line 982](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:982). |
| Claude L7 — `/agent quality-manager` bypass | **RESOLVED** | Interactive selection is refused in [agent-switch/index.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/agent-switch/index.ts:57). |
| Claude L8 — generic `StepResult.childRun` scope | **RESOLVED as accepted scope** | The durable terminal stage returns the nested run reference in [durable-chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:334). |
| Round-2 Claude M2 / round-3 NEW-M1 — signals | **RESOLVED for the reported paths** | CLI aborts its controller in [main.ts](/Users/cosmos/Projects/cosmonauts/cli/main.ts:622); active host command groups are killed on signal in [quality-review-command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:11). |
| Round-2 Claude L1 / round-3 Codex #4; Codex NEW-4 — failing audit finding and classification | **RESOLVED** | A bound failing audit is a gate failure, with host-added ID, derived priority, location, category, severity and fix in [quality-review-launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:124); [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:544) does not make it a human item. |
| Round-2 Claude L5 — materials writable during checks | **OPEN, accepted on record; acceptance unsound as an evidence guarantee** | Materials are written in [quality-review-workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:340), while preparation and checks run before the root is sealed in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:279) and [line 352](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:352). Same-user check code can alter the diff or base copies before the QM reads them. |
| Round-2 Claude L6 — analysis results in ordinary spawn events | **RESOLVED** | Capture is conditional on quality-review context in [agent-spawner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/agent-spawner.ts:431). |
| Round-3 Codex #1; Codex NEW-2 / Claude NEW-L4 report residue — abandoned write loses findings | **RESOLVED for a completed report** | Sealing has a grace period in [quality-review-artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:165). On abandonment, the host changes the verdict and reason while retaining an indexed report’s sections in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:692); the test checks findings and a human item in both final report and plan summary at [quality-review-run.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:815). |
| Round-3 Claude NEW-L1 — doubled audit prefix | **RESOLVED** | Host human-item formatting recognizes the existing prefix in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:544). |
| Round-3 Claude NEW-L2 — retained clones after cancel | **PARTIAL** | Three-second default settlement grace is at [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:239); a still-live session retains its clone at [line 714](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:714), with no later reclamation path in this slice. |
| Round-3 Claude NEW-L3 — JSX text false positive | **OPEN, accepted** | Comment collection can still classify JSX text at [suppression-policy.ts](/Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:42). Acceptance is sound as a known false positive: it overblocks rather than silently allowing a suppression. |
| Round-3 Claude NEW-L4 — panel skills omitted | **PARTIAL** | Missing clone skill locations are recorded in [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:77) and reported at [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:717), but the panel does not receive that skill. |
| Claude NEW-M1 — same-group process survives successful check | **RESOLVED** | Every command finish kills the group in [quality-review-command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:93). The background-marker test at [quality-review-command.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-command.test.ts:13) would fail with the old behavior. |
| Claude NEW-M2 — generated durable-chain boilerplate used as operator note | **RESOLVED** | The durable stage passes caller `stage.prompt` in [durable-chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:333); the no-prompt and caller-prompt cases are checked at [quality-review-launch.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-launch.test.ts:357). |
| Claude NEW-L2 — two-second removal timeout | **RESOLVED for the reported duration** | The default is 60 seconds in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:116); a failed or timed-out removal is exposed through [controller.ts](/Users/cosmos/Projects/cosmonauts/lib/durable-runtime/controller.ts:62). |
| Claude NEW-L3 — behavioral Markdown treated as docs | **RESOLVED for the named paths** | Capabilities, templates, `AGENTS.md`, and `CLAUDE.md` pass the behavioral filter in [quality-review-launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:351), with cases at [quality-review-launch.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-launch.test.ts:175). |

## Findings from the round-4 remediation

**HIGH — Base-owned argv still lets the change select the effective check program.** TASK-736 reads the `qualityReview` block from the merge-base in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:269) and passes its checks at [line 323](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:323). This prevents a changed `.cosmonauts/config.json` from replacing argv. This repository’s base argv includes `bun install --frozen-lockfile`, `bun run test`, `bun run lint`, and `bun run typecheck` in [config.json](/Users/cosmos/Projects/cosmonauts/.cosmonauts/config.json:24). A reviewed change to [package.json](/Users/cosmos/Projects/cosmonauts/package.json:29) can replace the `test` script with a successful no-op or another program; a changed test runner or install hook is another execution path. `package.json` is absent from the gate-owned list in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:972). The base argv test at [quality-review-run.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:132) changes only the config’s argv, so it cannot catch this case. This is an **incomplete D-025/TASK-736 remediation**, rather than a mechanism first introduced by this diff. It breaks D-025’s intended base ownership of what the host runs and B-008/INV-005 when a changed check script silences a gate. This finding concerns *selection of executed code*; it does not repeat N-004’s host authority issue.

**LOW — Path redaction misses an external host store root.** [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:227) supplies `projectRoot` and `run.runDir` to [redactOperatorNote](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:931). If a programmatic caller supplies a `FileRunStore` outside `projectRoot` and its note names that store’s root directory, neither replacement matches it. The note enters the QM prompt at [quality-review-launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:294). Default CLI, spawn and chain stores are beneath the source root and are covered by that replacement; the injectable store path is not. This breaks D-025’s no-host-store-path wording for that caller. The test at [quality-review-run.test.ts](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:2297) covers the default nested store path, not an external root.

I found no further new defect in TASK-736’s group cleanup, terminal-event restoration, audit-finding formatting, or abandoned-reviewer handling from the code and assertions inspected. I found no change that cancels timed-out panel children, installs a general evidence contract, adds leases, or changes the 200-character summary in [assistant-text.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/assistant-text.ts:34).

## End-to-end and accepted risks

CLI print and interactive QM calls, `spawn_agent`, inline and durable terminal chain stages all reach the shared launcher: [main.ts](/Users/cosmos/Projects/cosmonauts/cli/main.ts:653), [spawn-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:748), [chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:435), and [durable-chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:333). `/agent quality-manager` is refused. The host takes a private clone, computes the merge-base, binds source consent to that clone, and requires an observed bound audit before `ready`. `run status` exposes the final artifact and post-terminal workspace disposition. The only intended operator-checkout write is the D-018 plan summary in [quality-review-run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:1043).

The **detached-grandchild acceptance is sound only in its stated narrow form**: the host wait is bounded; a separately detached process can remain. The **JSX acceptance is sound** because the error fails closed. The **materials-writable acceptance is not sound as an evidence-integrity guarantee**: review materials are writable while changed project code runs, and the QM later reads `full.diff` from that directory. That is separate from the already escalated N-004 decision about host filesystem authority.

This was a read-only code-and-test review. `git diff --check ea1fa2d..HEAD` passed and the branch was clean. I did not run tests, so I make no passing-suite claim. The analysis skill’s `analysis_status` tool is not registered in this session; no structural-analysis result is claimed. Stages 7–8 are outside this verdict.
