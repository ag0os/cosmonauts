---
kind: independent-review
plan: qm-chain-safety
channel: codex exec -m gpt-6-sol -c model_reasoning_effort=high --sandbox read-only
scope: round-3 remediation e6ffb25..(TASK-735 a9725bf) + resolution of mid-review-3
date: 2026-09-24
---

# Mid-branch review 4 — codex

The prompt is `mid-review-4-prompt.md`. Final message verbatim:

## Resolution of the earlier reviews

Statuses apply to the current branch. Related findings from the two round-3 reviews are grouped, with every finding named.

| Earlier finding | Status | Current evidence |
|---|---|---|
| Codex C1 / Claude H2 / round-2 M1: source or host paths in quality prompts | **PARTIAL** | Panel skill paths under the source root are remapped into the clone in [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:60). The new caller note can put the source path straight back into the QM prompt; see NEW-1. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:290) |
| C2 / H1: clone analysis consent | **RESOLVED** | The snapshot gets run-scoped authorization, passed into project-tools discovery. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:260), [project-tools](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/project-tools/index.ts:556) |
| C3 / round-2 N2 / L1: unbound or unobserved audit permits `ready` | **RESOLVED** | The launcher checks observed tool results and the literal base; host verdict logic blocks `ready` unless the state is `completed-bound`, including through injected `execute`. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:24), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:512) |
| C4 / Claude M2: specialist procedures and direct spawns | **RESOLVED in bundled prompts** | The [generalist](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/reviewer.md:1) and [security](/Users/cosmos/Projects/cosmonauts/bundled/coding/prompts/security-reviewer.md:1) prompts retain review procedures and direct-spawn scope rules; the performance and UX prompts do likewise. The installed-package qualification is below. |
| C5: markerless `chain_run` / `run_driver` | **RESOLVED** | Both reject unresolved callers. [chain-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/chain-tool.ts:127), [driver-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/driver-tool.ts:195) |
| C6 / round-2 L4 / round-3 Codex #3 / Claude NEW-M2: host lens triage | **RESOLVED for the reported cases** | The filter now includes code under `memory`, removed lines, and behavioral prompt markdown. Tests cover deletion-only auth changes and docs-only changes. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:343), [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-launch.test.ts:175) |
| C7 / Claude M3 / round-2 N1: deadline, cancellation, live QM | **RESOLVED for the reported case** | Cancellation reaches the assessment; an unsettled QM is recorded and its workspace retained. The settle grace is now configurable. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:352), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:415) |
| Claude H3: base was the ref tip | **RESOLVED** | The clone computes the merge-base before creating materials and the diff. [workspace.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-workspace.ts:302) |
| Claude M4 / accepted detached grandchild: check timeout wait | **PARTIAL** | Finishing on leader `exit` and destroying pipes bounds the **host wait** even if a detached grandchild holds stdout. The grandchild itself can survive. [command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:93) |
| Claude M5 / round-2 L3 / round-3 Codex #2: cleanup and terminal ordering | **RESOLVED for the reported wait** | The final report and terminal event precede removal; removal has a timeout and a later disposition record. `run status` recognizes the terminal QM event. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:714), [status.ts](/Users/cosmos/Projects/cosmonauts/lib/durable-runtime/status.ts:19) |
| Claude M6 / round-2 H1: suppression forms and template regression | **RESOLVED** | Parser-based comment collection is restored; tests cover directives after template substitutions and compare tracked directives with the registry. [suppression-policy.ts](/Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:36), [test](/Users/cosmos/Projects/cosmonauts/tests/quality/suppression-policy.test.ts:126) |
| Claude L1: own plan summary enters capture | **RESOLVED** | Capture excludes this run’s summary path. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:250) |
| Claude L2: model-authored `refused` | **RESOLVED** | The host treats it as a report-integrity failure. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:485) |
| Claude L3 / round-2 N3 and L2: late reviewer evidence | **PARTIAL** | New writes are refused after sealing, but an already-started event append can complete after the terminal event; see NEW-3. [artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:143) |
| Claude L4: caller prompt and model dropped | **PARTIAL** | CLI print, `spawn_agent`, and chain prompts now travel as an operator note. The requested model still does not propagate; Stage 7 is outside this review. [cli/main.ts](/Users/cosmos/Projects/cosmonauts/cli/main.ts:653), [spawn-tool.ts](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:748) |
| Claude L5: production `execute` port | **PARTIAL** | The port remains in production options, but a missing gate state blocks `ready`. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:78), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:512) |
| Claude L6: unrelated config edit becomes gate-owned | **RESOLVED** | Only a changed `qualityReview` block adds that item. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:330), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:915) |
| Claude L7: `/agent quality-manager` bypass | **RESOLVED** | Typed selection is refused. [agent-switch](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/agent-switch/index.ts:50) |
| Claude L8: generic `StepResult.childRun` scope | **RESOLVED as accepted scope** | The durable QM stage uses it for the nested run reference. [durable-chain-runner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:334) |
| Round-2 Claude M2 / round-3 NEW-M1: signal handling | **RESOLVED for the reported paths** | CLI signals abort its controller; a host without its own listener kills active command groups and re-raises the signal. [cli/main.ts](/Users/cosmos/Projects/cosmonauts/cli/main.ts:622), [command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:11) |
| Round-2 Claude L1 / round-3 Codex #4: failing audit report | **PARTIAL** | The amended D-025 classification is correct: a bound failing audit is a gate failure, with no human item. Host findings now carry `file:line`, category and severity, but omit other AC-008 fields; see NEW-4. [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:121) |
| Round-2 Claude L5: materials writable during checks | **OPEN, accepted residual** | Materials remain writable until checks finish. More seriously, changed project code and check configuration execute with host filesystem authority; acceptance is assessed below. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:267), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:339) |
| Round-2 Claude L6: analysis results in ordinary spawn events | **RESOLVED** | Capture requires a quality-review context. [agent-spawner.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/agent-spawner.ts:431) |
| Round-3 Codex #1: unbounded reviewer sealing | **PARTIAL** | The writer wait now has a grace period, but abandonment discards the assessment report and an in-flight event append can land later; see NEW-2 and NEW-3. [artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:163) |
| Round-3 Claude NEW-L1: doubled audit prefix | **RESOLVED** | Real validator prefixes are recognized before adding the human item. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:533) |
| Round-3 Claude NEW-L2: clones retained on ordinary cancel | **PARTIAL** | The default settle grace rose to three seconds, reducing routine retention. A session still live after grace retains its clone, with no later reclamation path in this slice. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:230), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:718) |
| Round-3 Claude NEW-L3: JSX text false positive | **OPEN, accepted residual** | The parser-based collector can still classify JSX text as a comment. It overblocks; it does not silently allow a suppression. [suppression-policy.ts](/Users/cosmos/Projects/cosmonauts/lib/quality/suppression-policy.ts:42) |
| Round-3 Claude NEW-L4: panel skills silently omitted | **PARTIAL** | Missing clone skill locations are listed in the final report, but the skill remains unavailable to that panel session. [session-factory.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/session-factory.ts:74), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:692) |

## NEW findings in `e6ffb25..HEAD`

1. **HIGH — A caller note can reveal the operator checkout to a quality session.** [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:290) interpolates arbitrary `operatorNote` into the QM prompt. CLI `--print` passes its required prompt through unchanged at [main.ts](/Users/cosmos/Projects/cosmonauts/cli/main.ts:653); `spawn_agent` and chain stages do likewise. A prompt such as “review `/Users/cosmos/Projects/cosmonauts/lib/a.ts`” gives the QM the source root verbatim and its read tool can use absolute paths. Calling the note “non-authoritative” does not enforce the D-025 requirement that **no quality-session prompt name the source root or host store**. This breaks D-025 and D-004’s no-path-back boundary. The CLI test checks note propagation, not path exclusion. [test](/Users/cosmos/Projects/cosmonauts/tests/cli/main.test.ts:50)

2. **HIGH — Abandoning a reviewer write discards the completed findings report.** If a reviewer store write exceeds the sealing grace, [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:679) replaces `markdown` with a fresh failure report containing only the abandonment reason. A QM assessment that already recorded a security finding, failed check, or human item loses those details from `final.md` and the plan summary. The new test asserts the abandonment reason and failed status, but not preservation of assessment evidence. [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:747) This breaks INV-003, AC-008, B-004 and B-005: a failed assessment still needs an actionable durable report.

3. **MEDIUM — An abandoned reviewer can append an artifact event after finalization.** A write may already be inside `await store.appendEvent(...)` when sealing times out. Sealing marks it abandoned and removes its file, then the host finalizes; if the append later completes, [artifacts.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-artifacts.ts:143) records `artifact_written` *before* its next abandonment guard removes the file. `run status` collects artifact refs from those events, so it can advertise a missing reviewer file. [controller.ts](/Users/cosmos/Projects/cosmonauts/lib/durable-runtime/controller.ts:60) The append-never-settles test cannot catch a delayed append that eventually finishes. [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-artifacts.test.ts:120) This breaks B-003/AC-006’s evidence integrity and the sealed terminal lifecycle.

4. **MEDIUM — Host-inserted failing-audit findings are incomplete.** [launch.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-launch.ts:159) formats each envelope finding with its first location, category and severity, but drops its `id` and supplies no priority; the suggested fix disappears when `actions` is empty. If the model omitted that introduced finding, the host-added line is the only finding in `final.md`. The test checks the shortened strings as success. [test](/Users/cosmos/Projects/cosmonauts/tests/orchestration/quality-review-run.test.ts:317) The amended D-025 classification is satisfied, but AC-008 and B-005 still require every finding’s ID, priority and suggested fix.

## End-to-end and accepted risks

All named entry points route to the shared launcher: CLI print and interactive, QM `spawn_agent`, and inline or durable terminal chain stages. [CLI](/Users/cosmos/Projects/cosmonauts/cli/main.ts:653), [spawn](/Users/cosmos/Projects/cosmonauts/domains/shared/extensions/orchestration/spawn-tool.ts:748), [inline](/Users/cosmos/Projects/cosmonauts/lib/orchestration/chain-runner.ts:443), [durable](/Users/cosmos/Projects/cosmonauts/lib/orchestration/durable-chain-runner.ts:334) The launcher binds clone analysis consent, uses the merge-base for materials and checks, and requires a host-observed bound audit for `ready`. `/agent quality-manager` is refused. The intended host code writes to the operator checkout only for the D-018 plan summary, and `run status` points to `qm/final.md`. The path guarantee fails through NEW-1.

**The materials-writable acceptance is unsound as an INV-001 guarantee.** The host loads `qualityReview.prepare` and `checks` from the **reviewed clone** and executes them as host processes. [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:267), [run.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-run.ts:292), [command.ts](/Users/cosmos/Projects/cosmonauts/lib/orchestration/quality-review-command.ts:58) A changed config can specify `bun -e` with an absolute write to the operator checkout; changed test or install scripts can do the same. `cwd` in a private clone and `shell: false` do not confine filesystem writes. The changed `qualityReview` block is identified as gate-owned **after** those commands run. This is a pre-existing Stage 5–6 issue, separate from the NEW diff findings, and contradicts INV-001/AC-003’s “by construction” guarantee.

**The detached-grandchild acceptance is now sound in its stated narrow form:** the host wait ends on leader `exit`; only the grandchild itself can leak. **NEW-L3’s acceptance is sound** as a known false positive that fails closed.

One live-machine qualification remains: the installed `coding` package currently contains an older QM definition and takes precedence over the bundled one. [runtime.ts](/Users/cosmos/Projects/cosmonauts/lib/runtime.ts:36), [installed definition](/Users/cosmos/.cosmonauts/packages/coding/agents/quality-manager.ts:3) The bundled specialist prompts support both QM-panel and direct `cody`/`cosmo` spawns, but a live caller here may load the installed older prompts until that package is refreshed.

The diff passes `git diff --check`, and the branch stayed clean. I read code and test assertions; I did not run the test suite, the QM, a chain, or Drive. I found no change here that cancels timed-out panel children, adds a general evidence contract, or expands the 200-character summary contract. Stages 7–8 are outside this verdict.

**Verdict: DO-NOT-SHIP-YET for Stages 1–6 as remediated.** NEW-1 and NEW-2 are sufficient blockers; the pre-existing host-check authority gap independently prevents the ratified operator-checkout guarantee.
