You are an independent correctness and liveness reviewer for the Cosmonauts plan `qm-chain-safety`. READ-ONLY review: do not modify any file, do not run git commands that change state, do not run the Quality Manager or any cosmonauts chain/drive.

Repository: /Users/cosmos/Projects/cosmonauts, branch `feature/qm-chain-safety`. Stages 1–6 were implemented (`e43c238..b437cd1`) and reviewed by two independent channels; their findings are in `missions/plans/qm-chain-safety/mid-review-1-*.md` and `mid-review-2-*.md` (round 2 re-reviewed TASK-729..732). The coordinator recorded plan decision D-025 (in `missions/plans/qm-chain-safety/plan.md`) and remediation tasks TASK-729..732 and then TASK-733, TASK-734 (`missions/tasks/TASK-73*.md`); round-2 remediation is commits `e6c89ff` (TASK-734) and `4fbf361` (TASK-733) (diff `a978dbc..HEAD`, ignoring `missions/` except as documentation). Stages 7–8 (model diversity + closure calibration; legacy archive + callers) are NOT implemented yet — do not report their absence.

Authoritative: `missions/plans/qm-chain-safety/spec.md` (INV-001..005 and ACs ratified) and `plan.md` (B-001..B-012, D-001..D-025, Design, Risks).

Do three things:
1. For EVERY finding in the two mid-review-2 files (and any round-1 finding they marked PARTIAL or OPEN), state whether it is now RESOLVED, PARTIAL or OPEN, with file:line evidence.
2. Review the round-2 remediation diff `a978dbc..HEAD` for NEW defects it introduced: correctness, liveness (hangs, leaked processes or workspaces, lost verdicts), concurrency, error paths, tests that cannot fail, scope creep beyond D-025 and the tasks, and conflicts with ratified ground (INV-001..005, D-018..D-023) or with execution-liveness's ratified AC-015/AC-016/AC-018 (timed-out children never cancelled; no general evidence contract; 200-char summary unchanged).
3. Re-check end-to-end for real callers (CLI `cosmonauts -a coding/quality-manager`, `spawn_agent` of the QM, terminal QM stage of `chain_run` and named inline chains, `/agent quality-manager`, `run status`) that: analysis consent is actually bound in the clone; no quality session is given the source root or host run store path; `ready` needs a host-observed bound audit; the base is the merge-base; the operator checkout is unchanged except the D-018 plan summary; the specialist prompts serve both QM-panel and direct `cody`/`cosmo` spawns.

Verify claims by reading code and tests. Output: resolution table for step 1, then severity-ranked NEW findings (HIGH/MEDIUM/LOW) with file:line, concrete failing scenario and the AC/B/D broken, then a SHIP / DO-NOT-SHIP-YET verdict for Stages 1–6 as remediated. The coordinator accepted with record: Claude round-2 L5 (materials writable during checks), the residual own-process-group grandchild, and round-1 L4 remainder (caller prompt/model dropped; model is Stage 7) — say whether each acceptance is sound.
