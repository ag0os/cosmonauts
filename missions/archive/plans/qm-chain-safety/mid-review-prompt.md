You are an independent correctness and liveness reviewer for the Cosmonauts plan `qm-chain-safety`. This is a READ-ONLY review: do not modify any file, do not run git commands that change state, do not run the Quality Manager.

Repository: /Users/cosmos/Projects/cosmonauts, branch `feature/qm-chain-safety`. Review the diff `git diff e43c238..HEAD` (the base `e43c238` is the plan's last pre-implementation commit; ignore changes under `missions/` except where they document behavior). Stages 1–6 are implemented: TASK-720 (authority on chain_run/run_driver), TASK-722 (suppression check), TASK-721 (baseline-aware audit + docs), TASK-723 (QM run allocation, lifecycle, host artifacts), TASK-724 (private snapshot, materials, preparation), TASK-725 (review-only QM pass: restricted profile, host-run checks, spawn_agent panel capture, one-pass prompts). Stages 7–8 (model diversity + calibration; archive legacy + callers) are NOT implemented yet — do not report their absence.

Authoritative documents: `missions/plans/qm-chain-safety/spec.md` (Intent INV-001..005 and ACs are ratified) and `missions/plans/qm-chain-safety/plan.md` (behaviors B-001..B-012, Decision Log D-001..D-024, Design §1–§8, Risks R-001..R-014). Task files: `missions/tasks/TASK-72*.md`.

Assess whether the implemented code actually delivers, for real callers (CLI `cosmonauts -a coding/quality-manager`, `spawn_agent`, `chain_run`, named chains, `run_driver`, `run status`):
1. B-001 authority: every agent-starting route enforces the caller's subagents list (sequential, bracket, fan-out, named chains expanded inside chain_run), unknown caller/target fails closed, top-level CLI unchanged; a QM session cannot start fixer/coordinator/worker/verifier/integration-verifier and has no bash/edit/write/chain_run.
2. B-002 snapshot: plumbing-only source reads with GIT_OPTIONAL_LOCKS=0, private clone (not linked worktree), re-sample stability, refusal cases, operator checkout byte-identical after the run, QM and panel cwd is the clone, no path back to source root or host run store is visible to any agent session (D-004, D-006, D-012, R-012) — check the prompts and tool arguments the host hands to sessions.
3. B-003/B-004/B-005 evidence and lifecycle: reviewer evidence persisted by host before completion delivery; missing/duplicate/foreign/empty/timeout fails; conservative final.md written before fallible work; atomic replace; terminal matrix (Design §5); plan summary under missions/plans/<slug>/qm-runs/<runId>.md never overwrites; status pointers; lifecycle phases.
4. B-006 one pass and no remediation; AC-016: the QM's project checks, direct gate resolution, panel triage and specialist lenses keep working. Compare the reviewer/specialist prompts in bundled/coding/prompts/ with their versions at e43c238 and judge whether specialist lens capability was preserved. Note that `cody` and `cosmo` also spawn these reviewer agents directly outside the QM.
5. B-007/B-008 gates and suppression check, D-009 gate-owned-file human items.
6. Concurrency, cleanup, error paths, dead code, and anything that would hang, leak a workspace, lose a verdict, or let a review mutate the operator checkout.
7. Scope: flag anything outside the plan's behaviors, or excluded items (generic WorktreeSpec isolation, panel graph, owner-liveness, cancellation of timed-out children, 200-char summary change).

Verify claims by reading code and tests; run focused tests with `bun run test <path>` if useful (tests are read-only on the repo). Output: a severity-ranked list of findings (HIGH/MEDIUM/LOW), each with file:line, the concrete failing scenario, and which AC/B/D it breaks; then a SHIP / DO-NOT-SHIP-YET verdict for Stages 1–6.
