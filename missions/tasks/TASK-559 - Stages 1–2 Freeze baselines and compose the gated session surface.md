---
id: TASK-559
title: 'Stages 1–2: Freeze baselines and compose the gated session surface'
status: To Do
priority: high
labels:
  - backend
  - testing
  - devops
  - 'plan:knowledge-surface'
dependencies: []
createdAt: '2026-08-19T18:34:55.210Z'
updatedAt: '2026-08-19T18:34:55.210Z'
---

## Description

Deliver Slice A Stages 1–2 and own B-005, B-008, and B-012. First characterize the pre-feature seed, prompt, package, tool, wrapper, transition, and backfill-input baselines, then implement the config/session/package composition against those frozen fixtures. Tests must be observed RED before implementation, then GREEN, followed by refactoring.

The default-OFF declaration, AC-007 as amended by human ruling D-009, and the D-010/D-015 boundary are ratified stop-and-escalate ground: only literal `true` enables the surface; OFF identity covers gated-attributable discovery/tools/retrieval/context/store/filesystem effects, while only the exact distiller/archive/project-context-doc correction allowlist may change prompt content; enabled reach is every Cosmonauts-assembled session, not bare Pi hosts; generic tools/backends remain trusted and unsandboxed. Do not silently narrow these promises. Preserve the exclusions: no consolidation, working state, episode/explicit-save changes, embeddings, retention, autonomy-host behavior, cache/registry correctness state, or default enablement.

<!-- AC:BEGIN -->
- [ ] #1 Frozen fixtures capture the pre-feature prompt/package/tool/wrapper/authorization/config-transition baselines, all 36 markdown and 10 bundle seed inputs with raw timestamps/digests, and repository-derived backfill inputs; prompt comparison isolates exactly the D-009 content-correction allowlist.
- [ ] #2 The project knowledge-surface gate resolves enabled only for literal `true`; absent, false, or malformed values resolve OFF, and OFF assembly preserves original extension paths, adds no inline factory, and constructs no knowledge adapter or store.
- [ ] #3 B-005 is GREEN across initial CLI, `/agent` switch, and spawned sessions for every built-in/installed definition plus synthetic eligible/ineligible exact-wrapper fixtures: one functional `recall`, preserved registered-but-unauthorized legacy tools and identity guards, no unauthorized context/store calls, unrelated tools callable, and actionable collisions naming both recall-owner paths.
- [ ] #4 B-008 is GREEN against frozen OFF baselines and the exact D-009 allowlist: gated-attributable schemas/messages/results/filesystem effects stay inert, reload/plain-new retain frozen policy in both directions, and restart or `/agent` switch adopts OFF→ON and ON→OFF edits.
- [ ] #5 B-012 is GREEN for bare Pi package auto-loading while OFF: no knowledge factory/proposal adapter/tool is discovered and the thin shared wrapper surface matches baseline, with no enabled bare-host promise introduced.
- [ ] #6 Enabled composition uses one named inline extension supplied only by Cosmonauts assembly, substitutes only provenance-proven exact shared wrappers, keeps registration separate from authorization, and leaves thin package wrappers policy-free; no WeakMap, module singleton, EventBus correctness handoff, sandbox, or custom runtime is added.
- [ ] #7 Executable tests for B-005, B-008, and B-012 carry the exact `@cosmo-behavior plan:knowledge-surface#B-###` markers, are run RED before implementation and GREEN after refactor, and the affected project correctness evidence passes without changing any shipped prompt/skill outside the stack-agnostic D-009 allowlist.
<!-- AC:END -->
