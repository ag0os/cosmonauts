---
id: TASK-724
title: Stage 5 Private snapshot materials and preparation
status: To Do
priority: high
labels:
  - backend
  - devops
  - testing
  - 'plan:qm-chain-safety'
dependencies:
  - TASK-723
createdAt: '2026-09-24T03:16:46.677Z'
updatedAt: '2026-09-24T03:16:46.677Z'
---

## Description

Implement Implementation Order Stage 5 only: capture a stable source state without writes, materialize it as a private local clone, prepare complete review materials and dependencies, and map existing analysis consent in memory. This task solely owns B-002.

Binding ratified ground (not worker-adjustable; any collision requires halt-and-escalate with a drafted decision under the deviation protocol): INV-001 review and all children cannot mutate the reviewed checkout and isolation failure refuses; INV-002 every relied-on record belongs uniquely to its run and missing evidence fails; INV-003 a complete verdict persists on every exit; INV-004 authority lists bind every launch path; INV-005 changed-scope gates judge only introduced findings and new suppressions require a human-listed exception. D-001 requires: review-only QM with remediation through tasks, Drive and independent review; run-scoped full reports plus an every-exit plan summary and archived shared rounds; a private local clone now with generic runner isolation/OS sandbox deferred; introduced-only committed baselines and reconciled debt docs; human-only suppression exceptions; measured/reproduced evidence for performance P1 and independent closure; a different-family reviewer; and `chain_run` enforcement of the caller allowlist. D-002 forbids QM verification of this plan: implementation workers use codex `gpt-6-sol` at medium effort and closure uses a Claude subagent plus `codex exec -m gpt-6-sol -c model_reasoning_effort=high --sandbox read-only`, framed as correctness/liveness. D-018 permits exactly one reviewed-checkout change, the host-written new non-overwriting plan summary hidden from agents. D-019 makes missing checks/model config visible not-configured and human-decision items that block `ready`, never silence or refusal. D-020 requires a private local clone, not a linked worktree. D-021 starts the QM run after framework bootstrap at the launch boundary and before any QM/panel session. D-022 repairs only live-surface old links, preserves frozen/curated/evidence/archive history, and requires the archive README map. D-023 requires execution-liveness, when rebased later, to register prepare/check processes and the clone as outer-QM-attempt descendants.

<!-- AC:BEGIN -->
- [ ] #1 B-002 is owned here at direct CLI QM, spawned QM, and named/raw terminal-chain entry points: “The review sees the captured uncommitted state, including untracked files as additions. After the run, the operator checkout's HEAD, refs, `.git/index` bytes, tracked files, uncommitted edits and untracked files are byte-identical to before. That holds even when the stat cache was stale at capture. The only exception is the D-018 plan summary. An unsupported layout, unsafe symlink, unstable capture, failed dependency preparation, a QM in a non-terminal or parallel position, or a setup failure each produce a persisted refusal or failure report with a named reason, never a run in the shared checkout.”
- [ ] #2 D-003, D-020, D-021 and Design §2 are preserved: snapshotting happens at the resolved-role QM launch/stage boundary after bootstrap but before any QM/panel session; a bounded failed capture refuses without fallback; isolation is a disposable private local clone, never linked worktree or generic scheduler isolation; need for scheduler worktrees, merge finalizers, panel graph migration, or mutable parallel execution triggers the R-002 stop-and-escalate condition.
- [ ] #3 D-004 capture/materialization is exact: source Git reads use `GIT_OPTIONAL_LOCKS=0` and plumbing only to sample HEAD, refs, index identity, tracked/non-ignored-untracked paths, lstat, bytes/symlinks and deletions; a second identical sample is required; the clone receives the resolved base as a local ref before `origin` removal, detaches at captured HEAD, and overlays bytes/modes/safe relative links/deletions. R-003 refuses unsafe/root-escaping links, sparse layouts, unreproducible gitlinks/submodules, rapid instability and unusual unsupported nesting; R-004 makes any porcelain/source-writing command a failure.
- [ ] #4 D-004 review materials and preparation are complete: a sibling read-only materials root contains literal base SHA, range, changed-file list, full base-to-captured diff including untracked additions, and base copies of changed files; validated `qualityReview.prepare` argv runs host-side in the clone before checks, records duration, and names failures. R-006 permits only an outside-checkout lockfile cache if budget evidence later requires it, never a link to operator dependencies.
- [ ] #5 Consent is mapped through `domains/shared/extensions/project-tools/analysis-consent.ts` only after verifying the source real path's existing user consent and only as one-run in-memory authority for its verified snapshot; no consent record is copied or written, and R-007 treats any reusable/persisted/wider grant as an abort.
- [ ] #6 The Stage 5 file seam is complete in new `lib/orchestration/quality-review-workspace.ts`, the workspace/prepare composition in `quality-review-launch.ts` and `quality-review-run.ts`, one-run consent support in `analysis-consent.ts`, and `qualityReview.prepare` schema/example support in `lib/config/types.ts`, `lib/config/loader.ts`, `.cosmonauts/config.json`, and `.cosmonauts/config.example.json`. No source-root, host-run-store, mount, dependency symlink, or other path back is exposed. Per D-023/R-008, the clone and host preparation process remain explicit descendants for the later execution-liveness amend-on-record; this stage adds no leases, owner identity, start registration, settlement, or owner-death protocol.
- [ ] #7 Code delivery follows red → green → refactor with Vitest tests under `tests/` mirroring source and behavior tests through all shipped QM entry points; mutation-style cases include stale stat cache, staged/unstaged/deleted/untracked content, source changes between samples, each unsupported layout, preparation failure, consent escape, accidental porcelain/index refresh, and attempted fallback.
- [ ] #8 (Compliance patch, 2026-09-24) D-013 workspace lifecycle: a `workspace-reserved` event naming the deterministic temp path is appended before any clone I/O, and `snapshot-ready` after the verified materialization. On `finalized`, and on every refusal or failure with no live child, the clone and the materials root are removed before `StepResult` is returned. Entry-point tests assert that the temp path is absent after ready, not-ready, refused and failed exits, and that a crash injected after reservation leaves a lifecycle naming the phase and the workspace.
- [ ] #9 (Compliance patch, 2026-09-24) Preparation outcomes are pinned. A `qualityReview.prepare` step that runs and exits non-zero or times out ends the run durable-`failed` with verdict `failed`, naming the step id (D-004). Only capture, layout, position or invalid-prepare-config refusals are blocked with verdict `refused` (Design §5 row 1).
- [ ] #10 (Compliance patch, 2026-09-24) Scope of this stage's byte-identity proof: Stage 5 proves that the host's own actions (capture, clone, overlay, materials, prepare, consent mapping) leave the operator checkout byte-identical, including under a stale stat cache, and that the QM session's cwd is the clone. The end-to-end case 'whatever the QM's agents did' (an adversarial QM or child attempting bash, write, chain_run or spawn) is owned by TASK-725 because the D-012 restrictions land there.
<!-- AC:END -->
