---
id: TASK-631
title: 'Stage 1: Remediate knowledge-index measurement fidelity'
status: To Do
priority: high
labels:
  - backend
  - testing
  - 'plan:living-memory-fidelity'
dependencies:
  - TASK-630
createdAt: '2026-09-07T15:35:38.827Z'
updatedAt: '2026-09-07T15:35:38.827Z'
---

## Description

Implement only Stage 1, Implementation Order steps 4-7, as one test-first commit so the required RED evidence is followed by GREEN and refactor without ever committing red. Own B-001, B-002, and B-003 and their executable markers. Owning seams and adapters are `lib/memory/types.ts` and `lib/memory/index.ts` public contracts; `lib/memory/knowledge-records.ts` canonical retrieval descriptor; `lib/memory/consolidation-sources.ts` production corpus render-input handoff; `lib/extensions/knowledge-surface/index-policy.ts` pressure policy; `lib/extensions/knowledge-surface/combined-context.ts` injection producer/renderer; and `lib/memory/living-memory.ts` pressure reporting and retirement gate, with their named tests.

Ratified-ground handling: the five common constraints below and the spec's INV-001/INV-004 and AC-001..AC-003/AC-009/AC-011 letter are stop-and-escalate ground. Derived mechanism may be amended on record only while preserving that ground. Reverse dependency direction, a second corpus scan, body-ceiling coupling, or weakening a parent assertion is a halt/redesign condition.

<!-- AC:BEGIN -->
- [ ] #1 (Quality Contract assertion 7; Implementation Order steps 2 and 21) Directory boundary: the task's working diff touches only `lib/memory/`, `lib/extensions/knowledge-surface/`, `cli/memory/` and their mirrored tests under `tests/`. No repo-wide dead-code sweep, no export demotion, no unrelated API cleanup, no documentation, configuration, architecture-record or parent-plan edit. A prior remediation task without this criterion went 18 files wide and was fully reverted.
- [ ] #2 (Quality Contract assertion 6; Implementation Order step 1) Live corpus guard: before and after the task, `find knowledge -type f | sort | xargs shasum -a 256 | shasum -a 256` run from the repository root reports `adc3ef70a5e75cad8813db9a3ab954d4da84ebfeafbb75703529048007469932` and `find knowledge -type f | wc -l` reports 237. The relative `knowledge` root is load-bearing. No task may modify, move or delete anything under live `knowledge/`, and no task may run a live retirement round or any non-dry-run write-capable memory command against it.
- [ ] #3 (Quality Contract assertions 5 and 7; Implementation Order step 2) Parent ownership: the parent markers `@cosmo-behavior plan:living-memory#B-012|B-016|B-021` and their plan-declared test names remain exact and parent-owned across all six carrier tests enumerated in the plan's Architecture Context. No frozen pin, receipt floor, retirement/byte authority, fail-closed model-output validation or existing behaviour marker is weakened or removed. Any commit changing `lib/memory/types.ts` re-pins its full-source SHA-256 in the profile-playbooks seam test in the same commit.
- [ ] #4 (Quality Contract assertion 7; Implementation Order step 7) Gates: `bun run test`, `bun run lint`, `bun run typecheck` and `git diff --check` all pass at the task's commit boundary. No test is committed red.
- [ ] #5 (Quality Contract assertions 7-8; Implementation Order step 21) D-026 is not reopened, re-litigated, or given another verification layer, and retirement pathname sequencing in `lib/memory/retirement-store.ts` is not touched.
- [ ] #6 (B-001, B-002, B-003; Quality Contract assertions 1-2; Implementation Order steps 4-6) The named executable counterexamples carry only `@cosmo-behavior plan:living-memory-fidelity#B-001`, `#B-002`, and `#B-003` respectively and are observed failing for the intended current defects before production edits: both round-7 fixtures make pressure `renderedBytes` equal the warning-aware injected UTF-8 bytes instead of approximately 12 KB versus 364 bytes or 368 bytes versus 12,358 bytes; missing exact render input reports `kind: "unusable"`, `targetSatisfied: false`, and `index-pressure-unusable` with no non-empty retirement application; and one malformed knowledge record produces equal warning-bearing injection/pressure bytes, including the calibrated approximately 331-byte artifact rather than zero measured bytes.
- [ ] #7 (Quality Contract assertions 1-2, 4, 6-7; Implementation Order steps 5-7) One frozen canonical descriptor supplies `text: ""`, `KNOWLEDGE_RECORD_TYPES`, scopes `["project", "user"]`, the same user-root rule, and projection from complete parsed metadata inventory plus retrieval warnings; the production corpus adapter supplies exactly one required records-plus-warnings `KnowledgeIndexRenderInput` from its existing bounded retrieval while judgment bodies remain byte-bounded; combined context and pressure use that descriptor, one single-argument renderer, and the same input contract; multiple providers are rejected, absent exact input is unusable, every post-collection result exposes measured-or-unusable pressure, `toIndexRecords` and superseded call forms have no use, no second retrieval or local reconstruction exists, `lib/memory` does not import the extension, both B-021 carriers retain their exact names/markers/strength with the oversized carrier moved to the canonical query and required input, and the B-001/B-002/B-003 plus exact parent B-021 permanent pack passes after refactor.
- [ ] #8 (Plan ## Behaviors Test fields; Quality Contract assertion 5) Each owned counterexample lands at its exact plan-declared file and test name: B-001 at `tests/memory/living-memory.test.ts` > `matches pressure to injection for both round-7 divergence directions`; B-002 at `tests/memory/living-memory.test.ts` > `marks pressure unusable instead of reporting a false fit without an exact render input`; B-003 at `tests/extensions/architecture-memory.test.ts` > `counts injected knowledge warnings in measured index bytes`. A counterexample landing under a different name leaves the plan's Test fields stale with nothing mechanical to catch it, because artifact conformance validates only the Test field's file path and marker presence in that file, never the test name.
<!-- AC:END -->
