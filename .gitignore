node_modules/
dist/
coverage/
packages/
bin/cosmo-*-claude
*.tsbuildinfo
.DS_Store

# Session transcripts: high-volume, low-diff-value, regenerable.
# Everything else under missions/ is tracked — we dogfood cosmonauts
# on itself. None of this ships to npm (tarball is scoped by package.json
# "files", which excludes missions/, memory/, and .cosmonauts/).
missions/sessions/
missions/archive/sessions/

.claude/

# Fallow local analysis cache. Baselines under .fallow/baselines/ are tracked.
.fallow/cache.bin
.fallow/churn.bin
.fallow/snapshots/
